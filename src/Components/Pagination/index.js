import React, { useState, useEffect } from 'react';

const [page, setPage] = useState(0);

function pagenation() {
  const onPage = Math.ceil(state.length / 50);
}

export default [pagenation, page];
